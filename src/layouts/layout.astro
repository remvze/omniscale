---
import '@/styles/global.css';

interface Props {
  description?: string;
  og?: string;
  title?: string;
}

const title = Astro.props.title
  ? `${Astro.props.title} — Omniscale`
  : 'Omniscale: The Scale of Everything in the Universe';

const description =
  Astro.props.description ||
  'This scale shows the size of everything in the universe, from the largest galaxies to the smallest particles.';

const og = Astro.props.og || '/og.png';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta content="width=device-width" name="viewport" />
    <link href="/favicon.svg" rel="icon" type="image/svg+xml" />
    <meta content={Astro.generator} name="generator" />
    <meta content="#18181b" name="theme-color" />

    <title>{title}</title>
    <meta content={title} name="title" />
    <meta content={description} name="description" />

    <meta content={title} property="og:title" />
    <meta content={description} property="og:description" />
    <meta content="Omniscale" property="og:site_name" />
    <meta content="https://omniscale.mvze.net/" property="og:url" />
    <meta content="website" property="og:type" />
    <meta content={`https://omniscale.mvze.net${og}`} property="og:image" />

    <link href="/sitemap-index.xml" rel="sitemap" />
  </head>
  <body>
    <slot />
  </body>
</html>
